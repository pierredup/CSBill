# https://packager.io/documentation/customizing-the-build/

name: CSBill
user: "apache"
group: "apache"
buildpack: "https://github.com/heroku/heroku-buildpack-php#master"
  
dependencies:
  - readline
  - readline-devel
  
targets:
#  debian-7:
#    - libreadline5
#    - libreadline-dev
  ubuntu-14.04:
    - libreadline5
    - libreadline-dev
# turn off emails for now
notifications: false

# Build and protect (this is before distribution)
# https://packager.io/documentation/customizing-the-build/#before\
