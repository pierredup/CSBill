buildpack: https://github.com/pierredup/heroku-buildpack-php

targets:
  centos-6:
    build_dependencies:
      - readline
      - readline-devel
    dependencies:
      - mysql
      - php-twig-ctwig
      
before:
    - python --version
    - ls -la /usr/bin
    - ls -la /usr/local/bin
